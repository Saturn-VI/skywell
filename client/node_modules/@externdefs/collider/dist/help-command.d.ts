import type { RunState } from './core/index.js';
import { Command, type CommandClass } from './command.js';
export declare class HelpCommand extends Command {
    private readonly contexts;
    private commands;
    private index?;
    static from(state: RunState, contexts: CommandClass[]): HelpCommand;
    constructor(contexts: CommandClass[]);
    execute(): Promise<void>;
}
