import { type CommandBuilder, type RunState } from '../core/index.js';
import { Option, type OptionReturn } from './base.js';
export type RestOptionFlags = {
    name?: string;
    required?: number;
};
export declare function Rest(options?: RestOptionFlags): OptionReturn<string[]>;
export declare class RestOption extends Option<string[]> {
    name: string | undefined;
    required: number | undefined;
    constructor(options: RestOptionFlags);
    define(builder: CommandBuilder<unknown>, key: string): void;
    transform(builder: CommandBuilder<unknown>, _key: string, state: RunState): string[];
}
