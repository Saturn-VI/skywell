import { Option } from './base.js';
export function Proxy(options = {}) {
    return new ProxyOption(options);
}
export class ProxyOption extends Option {
    name;
    required;
    constructor(options) {
        super();
        this.name = options.name;
        this.required = options.required;
    }
    define(builder, key) {
        builder.addProxy({
            name: this.name ?? key,
            required: this.required,
        });
    }
    transform(_builder, _key, state) {
        return state.positionals.map((pos) => pos.value);
    }
}
//# sourceMappingURL=proxy.js.map