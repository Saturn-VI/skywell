import { Command } from '../command.js';
/**
 * A command that prints the version of the binary (`program.binaryVersion`).
 *
 * Paths: `-v`, `--version`
 */
export class VersionCommand extends Command {
    static paths = [[`-v`], [`--version`]];
    async execute() {
        console.log(`${this.program.binaryVersion ?? `<unknown>`}`);
    }
}
//# sourceMappingURL=version.js.map