import { Command } from '../command.js';
import { Proxy } from '../options/proxy.js';
/**
 * A command that prints the clipanion tokens.
 */
export class TokensCommand extends Command {
    static paths = [[`--clipanion=tokens`]];
    args = Proxy();
    async execute() {
        console.log(`${JSON.stringify(this.program.process(this.args).tokens, null, 2)}`);
    }
}
//# sourceMappingURL=tokens.js.map