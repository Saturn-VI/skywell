import { Command } from '../command.js';
/**
 * A command that prints the definitions.
 */
export class DefinitionsCommand extends Command {
    static paths = [[`--clipanion=definitions`]];
    async execute() {
        console.log(`${JSON.stringify(this.program.definitions(), null, 2)}\n`);
    }
}
//# sourceMappingURL=definitions.js.map