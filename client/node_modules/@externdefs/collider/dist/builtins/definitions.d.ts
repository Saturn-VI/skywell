import { Command } from '../command.js';
/**
 * A command that prints the definitions.
 */
export declare class DefinitionsCommand extends Command {
    static paths: string[][];
    execute(): Promise<number | void>;
}
